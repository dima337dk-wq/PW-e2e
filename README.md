# Playwright Tests

Автоматизированные тесты для проекта Tallinn Delivery на Playwright + TypeScript.

## Настройка проекта

npm install

Создаём файл .env с переменной APP_URL, которая содержит секретный URL

## Как запустить тесты

npx playwright test

На CI тесты запускаются автоматически